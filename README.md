# 🌸 Iris Species Prediction (Machine Learning Project)

โปรเจกต์นี้เป็นระบบทำนายสายพันธุ์ดอกไม้ Iris โดยใช้ข้อมูลจาก **Iris Dataset**  
โมเดล Machine Learning ที่ใช้ ได้แก่ Logistic Regression

---

## 📊 Dataset
Dataset ที่ใช้คือ **Iris.csv** ซึ่งประกอบด้วย 150 แถว และ 5 คอลัมน์

- SepalLengthCm  
- SepalWidthCm  
- PetalLengthCm  
- PetalWidthCm  
- Species (Label)

---

## 🛠️ Data Preprocessing

- โหลดข้อมูลด้วย Pandas  
- ตรวจสอบค่าที่หายไป (Missing values)  
- วิเคราะห์ข้อมูลเบื้องต้น (EDA) เช่น `describe()` และ `unique()`  
- แบ่งข้อมูลเป็น Train/Test ด้วย `train_test_split`

---

## 🤖 Machine Learning Models
โมเดลที่ใช้หลักในโปรเจกต์นี้คือ:

### 1. Logistic Regression  
- ใช้สำหรับ Classification 3 classes  
- แม่นยำดี เหมาะกับ dataset เล็ก ๆ อย่าง Iris  

### (สามารถเพิ่มโมเดลอื่นได้ เช่น)  
- Random Forest  
- K-Nearest Neighbors (KNN)  
- Support Vector Machine (SVM)

---

## 📈 Model Performance

- Precision  
- Recall  
- F1 Score  
- Support  

ตาราง Confusion Matrix ถูกใช้เพื่อดูว่าโมเดลทายผิดที่ตำแหน่งใด

---

## 🚀 How to Run

### 1. Clone โปรเจกต์
### 2. ติดตั้ง Library ที่จำเป็น
### 3. รัน Notebook
